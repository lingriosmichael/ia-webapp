import { t as Route } from "./analytics-Cg4jD3Ck.js";
import { useEffect } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx } from "react/jsx-runtime";
import { useTranslation } from "react-i18next";
//#region src/routes/projects/$projectId/activities/$activityId/analytics.tsx?tsr-split=component
function LegacyAnalyticsRedirect() {
	const { projectId, activityId } = Route.useParams();
	const navigate = useNavigate();
	const { t } = useTranslation();
	useEffect(() => {
		navigate({
			to: "/projects/$projectId/activities/$activityId/analysis",
			params: {
				projectId,
				activityId
			},
			replace: true
		});
	}, [
		activityId,
		navigate,
		projectId
	]);
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center text-sm text-muted-foreground",
		children: t("activityBrief.redirectingToOverview")
	});
}
//#endregion
export { LegacyAnalyticsRedirect as component };
