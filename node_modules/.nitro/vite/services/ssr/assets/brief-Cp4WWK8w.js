import { t as Route } from "./brief-COunA5k5.js";
import { useEffect } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx } from "react/jsx-runtime";
import { useTranslation } from "react-i18next";
//#region src/routes/projects/$projectId/activities/$activityId/brief.tsx?tsr-split=component
function LegacyBriefRedirect() {
	const { projectId, activityId } = Route.useParams();
	const navigate = useNavigate();
	const { t } = useTranslation();
	useEffect(() => {
		navigate({
			to: "/projects/$projectId/activities/$activityId/overview",
			params: {
				projectId,
				activityId
			},
			replace: true
		});
	}, [
		activityId,
		navigate,
		projectId
	]);
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center text-sm text-muted-foreground",
		children: t("activityBrief.redirectingToOverview")
	});
}
//#endregion
export { LegacyBriefRedirect as component };
