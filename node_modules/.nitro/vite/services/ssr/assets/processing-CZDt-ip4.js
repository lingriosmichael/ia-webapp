import { t as Route } from "./processing-B1XaKgdJ.js";
import { useEffect } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx } from "react/jsx-runtime";
import { useTranslation } from "react-i18next";
//#region src/routes/projects/$projectId/activities/$activityId/processing.tsx?tsr-split=component
function LegacyProcessingRedirect() {
	const { projectId, activityId } = Route.useParams();
	const navigate = useNavigate();
	const { t } = useTranslation();
	useEffect(() => {
		navigate({
			to: "/projects/$projectId/activities/$activityId/overview",
			params: {
				projectId,
				activityId
			},
			replace: true
		});
	}, [
		activityId,
		navigate,
		projectId
	]);
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center text-sm text-muted-foreground",
		children: t("activityBrief.redirectingToOverview")
	});
}
//#endregion
export { LegacyProcessingRedirect as component };
